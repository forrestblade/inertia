// Login form reactive bindings — powered by @valencets/reactive

/** Initialize reactive bindings on the login form. Returns dispose function. */
export function initLoginForm (_form: HTMLFormElement): () => void {
  return () => {}
}
