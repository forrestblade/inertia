# Inertia Studio — Production Environment
# Copy to /home/agent/inertia/.env on the Pi

STUDIO_PORT=5173
STUDIO_HOST=0.0.0.0

DB_HOST=localhost
DB_PORT=5432
DB_NAME=inertia_studio
DB_USER=inertia_app
DB_PASSWORD=changeme
DB_MAX_CONNECTIONS=10

ADMIN_TOKEN=changeme

NODE_ENV=production
